import { SectionHead } from "#/components/misc/SectionHead";

export const TrackMotifs = () => {
    return (
        <div>
            <SectionHead label="The Motifs" num={2} />
        </div>
    );
};
