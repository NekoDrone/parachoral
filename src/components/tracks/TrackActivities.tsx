import { SectionHead } from "#/components/misc/SectionHead";

export const TrackActivities = () => {
    return (
        <div>
            <SectionHead label="Plays in" num={3} />
        </div>
    );
};
